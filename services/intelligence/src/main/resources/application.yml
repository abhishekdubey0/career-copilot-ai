server:
  port: 8083

spring:
  application:
    name: resume-intelligence-service

  datasource:
    url: jdbc:postgresql://localhost:5432/intelligence_db
    username: postgres
    password: Abhi@1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: qwen2.5:3b

services:
  resume:
    base-url: http://localhost:8082